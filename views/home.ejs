<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= homePage %></p>

<% posts.forEach(post => { %> 
    <% const trim = post.message.slice(0,100) + "..."; %> 
    <h2><%= post.title %></h2>
    <p><%= trim %> 
    <a href= "/post/<%= post.title %>">Read more</a>
    </p>


<%}); %> 

<%- include('partials/footer'); -%>

